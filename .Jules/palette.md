## 2024-04-08 - Added ARIA attributes to icon-only toggle buttons\n**Learning:** Icon-only menu buttons without ARIA attributes or dynamic `aria-expanded` states provide no actionable feedback to screen reader users.\n**Action:** When implementing responsive mobile menus, always include `aria-label`, `aria-controls`, and dynamically toggle `aria-expanded` on the trigger button to clearly communicate state and intent.
